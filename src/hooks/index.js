// Barrel export for custom hooks
// This enables cleaner imports: import { useModalState, useElapsedTime } from '../hooks';

export { useModalState } from './useModalState';
export { useElapsedTime, useElapsedDuration } from './useElapsedTime';
